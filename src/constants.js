// Shared layout constants used by both JS logic and runtime CSS variables
export const GRID = 24;
// small tolerance (px) to allow exact-intersection checks in high-DPI environments
export const SNAP_TOLERANCE = 0.5;
